# 0.6.2 全量更新 · 执行编排

母方案：`2026-07-10-0.6.2-体验止血与漏斗疏通方案.md`（codex 二审通过）。
范围：P0 全部 + P1 全部；P2 维持"择机"不做。实现者：codex CLI（gpt-5.6-sol，已探测 MODEL_OK）；每阶段协议：**codex 实现 → Claude 亲审 diff → 亲跑测试 → commit**（禁 codex 审 codex）。

一票否决标准：发版后主漏斗任一现有功能回归（检索/入库/综述/GAP 走不通）= 任务失败。

## 阶段表

| 阶段 | 内容 | 对应母方案 | 提交 |
|---|---|---|---|
| S1 | R 代理响应模型全面 null 容错 + 契约/类型同步 + null 回归测试 | P0-1 | fix(schemas) |
| S2 | persona 承诺边界 + final_output 独白剥离 | P0-3 | fix(agent) |
| S3 | 共享 readiness selector + chat 入口空库发送前拦截 | P0-2 | feat(web) |
| S4 | precheck 卡内嵌一键补全文 + backfill 埋点 | P0-4 | feat(review) |
| S5 | feasibility 前端接线（Welcome 文案随之成真，免撤） | P1-1（吸收 P0-5） | feat(research) |
| S6 | 主漏斗埋点铺开 + 每日首访 | P1-2 | feat(analytics) |
| S7 | 检索完成状态化推荐 + 时间戳统一 | P1-3/P1-4 | feat(web)+fix(time) |
| S8 | bump 0.6.2 → 全量测试 → 备份+部署生产 → 生产走查 → tag | 发版 | chore(release) |

## 硬约束（每个 codex 阶段 prompt 均携带）

- 不 git commit（由编排方提交）；不动标注"校准值"的阈值；注释语言跟随现有代码（中文）。
- 后端测试统一 `pytest -m "not allow_real_llm_router"`（避免真跑 LLM）。
- 改响应契约必须三处同步：`schemas.py` ↔ `packages/contracts/openapi.yaml` ↔ `pnpm gen:api`；前端 e2e fixture 与后端 contract fixture 手动同步（虚绿风险）。
- 前端已知 pre-existing failure：settingsM5 8 fail，与本轮无关，不当回归。

## 部署（S8，沿用 0.6.1 机制）

备份 web+agent+schema → rsync agent（勿碰 .env/.venv）→ prod alembic upgrade head（本轮预期无新迁移）→ 本地 `VITE_API_BASE=/api pnpm build` → rsync dist（排除 media/aria-review.mp4）→ `systemctl restart aria-agent` → healthz/welcome/登录/主漏斗 Playwright 走查 → `/opt/aria-review` git commit + tag v0.6.2。
